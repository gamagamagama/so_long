# ⚠️ This file may trigger epileptic seizures.⚠️ 
## Viewer discretion is advised when viewing this file.

### Usage :

```bash
#to install dependencies on your machine
make deps  
```
```bash
#Clone and Build MLX42 lib + compile so_long
make all
```
```bash
#Usage : ./so_long map_file/map[0-13 or 42].ber
./so_long map_file/map11.ber
```
### Controls:

Use `WSAD` to move , try to Hit `M` for Master Mode

### if some command from above doesnt work feel free to examine Makefile wher you can find all links and commands.😉


